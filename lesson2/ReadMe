Внезапно перестали запускаться тесты cucumber(запускается тест и виснит).   разобраться в причинах самостоятельно не удалось.


Для корректной работы с моим сервером нужно внести изменения в клиенте в файл App.js
const move = function(x, y){
    axios.post(moveUrl, {
      x: x ,
      y: y
    }).then(updateField);
  }
